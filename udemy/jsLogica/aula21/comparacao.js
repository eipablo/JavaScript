/* 
? OPERADORES DE COMPARAÇÃO
* > maior que
* >= maior ou igual que
* < menor que
* <= menor ou igual que
! == igualdade (valor) <- só checa valor, e não é recomendado utilizar *********************
* === igualdade estrita (valor e tipo)
! !=diferente que (valor) <- só checa valor, e não é recomendado utilizar *********************
* !== diferente estrito (valor e tipo)
*/

const num1 = 2;
const num2 = 4;
const comp = num1 <= num2;

console.log(comp);